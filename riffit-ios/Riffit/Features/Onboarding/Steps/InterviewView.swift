import SwiftUI

/// Step 2 of onboarding: the AI interview.
/// A conversational chat interface where Claude asks the creator
/// questions to build their CreatorProfile. Branching is based
/// on the selected creator type.
struct InterviewView: View {
    var body: some View {
        // TODO: Implement AI interview chat UI (Phase 2, Step 7)
        Text("AI Interview")
    }
}
