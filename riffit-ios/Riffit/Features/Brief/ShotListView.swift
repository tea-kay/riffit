import SwiftUI

/// Displays the shot list for a content brief.
/// Each shot has a number, description, type, and notes.
struct ShotListView: View {
    let shots: [ContentBrief.Shot]

    var body: some View {
        // TODO: Implement shot list view (Phase 4, Step 19)
        Text("Shot List")
    }
}
